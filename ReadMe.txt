This is a dummy read-me file.

The real read-me is "README.md", which is written in the markdown format (.md).

Note that a markdown file is just a text file.

Open "README.md" with markdown editors (e.g. Boostnote and Visual Studio Code) or text editors (e.g. Notepad, Emacs, and TextEdit).
